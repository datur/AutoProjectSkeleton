# Sphinx InstructionsIf sphinx is not installed run:

	```pip install sphinx```

Once installed run `sphinx-quickstart`
Dont forget to fix the path in `conf.py` change it to something like `../<module >` or `..`